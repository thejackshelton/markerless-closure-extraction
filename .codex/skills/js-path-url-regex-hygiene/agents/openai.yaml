display_name: JS Path Hygiene
short_description: Minimal path, URL, and regex handling.
default_prompt: Use this skill when editing JS or TS code that handles paths, URLs, route-like strings, evidence paths, or regular expressions.
